.c-category-label {
    border: 1px solid var(--color-label);
    color: var(--color-label);
    text-align: center;
    display: inline-block;
    padding: 5px 10px;
    font-size: 12px;
    font-weight: bold;
}